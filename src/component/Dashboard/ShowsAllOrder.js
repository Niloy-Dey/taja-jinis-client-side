import React from 'react';

const ShowsAllOrder = ({allOrder}) => {
    const {name, image, } = allOrder;
    return (
        <div>
               
         
        </div>
    );
};

export default ShowsAllOrder;